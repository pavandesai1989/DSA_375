https://leetcode.com/problems/find-all-anagrams-in-a-string/description/
https://www.geeksforgeeks.org/problems/count-occurences-of-anagrams5839/1

// User function template for C++
class Solution {
  public:
    int search(string &pat, string &txt) 
    {
        unordered_map<char,int> mpt;
        unordered_map<char,int> mpp;
        int res = 0;
        
        //add ele of par in mpp
        for(auto& ele:pat) //O(P)
        {
            mpp[ele]++;
        }
        
        int l=0,r=0;
        int len_p = pat.size();
        int len_t = txt.size();
        
        while(r<len_t)  //O(T)
        {
            //add char of txt in mpt
            mpt[txt[r]]++;
            
            //check if len is equal to len of pat
            if(r-l+1 == len_p)
            {
                if(mpt == mpp)  //O(K) K number oof chars
                {
                    res++;
                }
                mpt[txt[l]]--;
                
                if(mpt[txt[l]] == 0)
                {
                    mpt.erase(txt[l]);
                }
                l++;
            }
            r++;
        }
        return res;
        
    }
};

time : O(P)+O(T×K)

Since K is constant (≤26 for lowercase letters):

space : mpp: up to K characters

mpt: up to K characters

𝑂
(
𝐾
)
O(K)
	​
